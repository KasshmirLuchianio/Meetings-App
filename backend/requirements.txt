# ==================== Core ====================
fastapi>=0.110.0
uvicorn>=0.25.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
starlette>=0.37.0
pydantic>=2.5.0

# ==================== Database ====================
motor>=3.3.0
pymongo>=4.5.0

# ==================== Local AI — Speech-to-Text ====================
# faster-whisper replaces Groq/OpenAI Whisper (zero cloud calls)
faster-whisper>=1.0.0
ctranslate2>=4.0.0

# ==================== Local AI — LLM (Ollama client) ====================
# Uses OpenAI-compatible endpoint — same SDK, different base_url
openai>=1.0.0

# ==================== AI Fallback (optional, for dev/migration) ====================
# Remove these in production air-gapped deployments
anthropic>=0.25.0

# ==================== Audio / Media ====================
# gTTS removed — was cloud-dependent and unused in core flow

# ==================== Export (PDF + DOCX) ====================
fpdf2>=2.8.0
python-docx>=1.1.0
lxml>=5.0.0
pillow>=10.0.0

# ==================== HTTP / Async ====================
aiofiles>=23.0.0
aiohttp>=3.9.0
httpx>=0.25.0
requests>=2.31.0

# ==================== Rate Limiting ====================
slowapi>=0.1.9

# ==================== Auth ====================
bcrypt>=4.1.0
passlib>=1.7.4
PyJWT>=2.8.0
cryptography>=41.0.0

# ==================== Utils ====================
Jinja2>=3.1.0
PyYAML>=6.0.0
python-dateutil>=2.8.0
numpy>=1.26.0

# ==================== REMOVED (cloud dependencies) ====================
# stripe          — replaced by license-key model for on-premise
# resend          — replaced by local SMTP (notifications/email.py)
# boto3           — S3 not used (local file storage)
# s3transfer      — same as above
# tiktoken        — only needed for OpenAI token counting
# dnspython       — not needed for on-premise
